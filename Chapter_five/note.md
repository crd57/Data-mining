# numpy

```python
arr.T # 转置
np.linalg.inv(arr) # 求逆
arr.cumsum(axis = 1) # 按行累加求和
np.vstack(arr1,arr2) # 纵向合并数组
np.hstack(arr1,arr2) # 横向合并数组
np.hsplit(arr,3) # 将数组横向拆分成3份
np.vsplit(arr,3) # 将数组纵向拆分成3份
np.all() # 是否全为True
np.any() # 是否有为True
np.sort() # 排序
np.std() #  
```